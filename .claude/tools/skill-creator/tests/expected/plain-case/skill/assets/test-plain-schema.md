# test-plain state JSON shape

Canonical state file:
`~/trade-imports-arch-workspace/.claude/workareas/test-plain/<id>/state.json`.

Mutated only via `tools/test-plain/*.sh` helpers. A markdown view is regenerated by the skill's render helper (if the helpers list includes one) whenever the JSON changes.

## Schema

```jsonc
{
  "id": "<id>",
  "created_at": "2026-05-26T...",
  "items": [
    {
      "id": 1,
      "TODO": "TODO - fill in the per-item shape"
    }
  ]
}
```

## Field rules

- TODO - document each field, validation rules, and which helper mutates it.
